## Packages
framer-motion | Layout animations and smooth transitions
recharts | Dashboard analytics charts
date-fns | Date formatting for tables and movements
lucide-react | Icons for the UI (already in base, but listing to be safe)
clsx | Class name conditional logic
tailwind-merge | Merging tailwind classes safely

## Notes
Integration with shared API via hooks
Movements require complex form state (dynamic items list)
Project inventory calculation happens on backend, frontend just displays
Theme: Industrial Professional (Slate + Amber)
